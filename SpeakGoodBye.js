// Define byeSpeaker object
/*
var byeSpeaker = {
  speak: function(name) {
    console.log("Goodbye " + name);
  }
};
*/

var speakWord = "Good Bye";
function speak(name) {
  console.log(speakWord + " " + name);
}
